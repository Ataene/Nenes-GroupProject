import React from 'react'
import Button from "@mui/material/Button";

const Postad = () => {
  return (
    <div>
      <Button
        variant="outlined"
        color="success"
        size="small"
        onClick={() => {
        }}
      >
        Post Ad
      </Button>
    </div>
  );
}

export default Postad;